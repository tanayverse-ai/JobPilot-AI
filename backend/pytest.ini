[pytest]
; Without this, pytest (default "prepend" import mode, no __init__.py in
; tests/) adds `tests/` to sys.path instead of `backend/`, so `import app`
; fails with "No module named 'app'" even though the app itself runs fine.
pythonpath = .
